package com.afinaufal.plugin.layout

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle

class RelativeLayout : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_relative_layout)

        // ralative layout
        // adalah layout simana pomposisian dari layout ini berdasarkan relative dari view di sekitarnya
        // ctrl + click (activity_relative_layout) untuk langsung ke layout

    }
}